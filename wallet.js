// Wallet routes for deposit and spend
const express = require('express');
const router = express.Router();
router.post('/deposit', () => {});
router.post('/spend', () => {});
router.get('/balance/:userId', () => {});
module.exports = router;